import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((theme) => {
  return {
    root: {
      marginTop: "30px",
      marginBottom: "100px",
      display: "flex",
      flexDirection: "row",
      justifyContent: "center",
    },
  };
});
